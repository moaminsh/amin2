# راهنمای جامع معماری امنیت و سیاست‌های حفاظتی (SECURITY.md)

این سند معماری امنیتی، مدل تهدید، سیاست‌های کنترل دسترسی و فرآیندهای نگهداری پنل مدیریت امن سامانه را تشریح می‌کند.

---

## ۱. اصول بنیادین و عدم تکیه بر پنهان‌کاری (No Security Through Obscurity)

1. **دسترسی صریح و احراز شده:** امنیت سامانه هرگز بر اساس مخفی بودن URL پنل مدیریت (`/admin`) یا پنهان نگه‌داشتن فایل‌ها تعریف نشده است؛ بلکه بر پایه لایه‌های احراز هویت (Authentication)، مجوزهای دسترسی نقش‌محور (RBAC)، امضای رمزنگاری نشست‌ها و توکن‌های ضد جعل CSRF تضمین می‌شود.
2. **عدم استفاده از Backdoor یا Web Shell:** هیچ‌گونه درگاه مخفی، پارامتر پنهان (مانند `?pass=secret`) یا دسترسی غیرقابل‌ردیابی در کدهای فرانت‌اند و بک‌اند وجود ندارد.
3. **تفکیک کامل متغیرهای حساس:** هیچ رمز عبور، کلید هشینگ، یا توکن امنیتی در سورس‌کد، فایل‌های عمومی یا مخزن گیت قرار داده نشده و تماماً از طریق متغیرهای محیطی سیستم (`.env`) تزریق می‌شوند.

---

## ۲. معماری احراز هویت و مدیریت نشست‌ها

### الف. ذخیره‌سازی و هشینگ رمزهای عبور
- از الگوریتم استاندارد **Bcrypt** با فاکتور کاری (Cost Factor) مناسب (۱۰ دور سالت) برای هشینگ استفاده می‌شود.
- در برابر حملات Timing Attacks با استفاده از `bcrypt.compare` و مقایسه‌های زمان‌ثابت محافظت شده است.
- پس از اولین ورود با رمز موقت اولیه، کاربر بلافاصله و به صورت اجباری ملزم به انتخاب یک رمز عبور قدرتمند جدید می‌شود (`requires_password_change = 1`).

### ب. احراز هویت دو مرحله‌ای (TOTP MFA / 2FA)
- پیاده‌سازی سازگار با Google Authenticator و استاندارد RFC 6238 (الگوریتم SHA-1 TOTP، بازه ۳۰ ثانیه‌ای).
- صدور ۸ عدد Backup Code یک‌بار مصرف با انتروپی بالا برای بازیابی اضطراری دسترسی در صورت مفقود شدن دستگاه احراز هویت.
- غیرفعال‌سازی ۲FA مستلزم ورود مجدد رمز عبور به علاوه کد ۲FA معتبر است تا از تصاحب نشست توسط افراد غیرمجاز جلوگیری شود.

### ج. امنیت نشست‌ها (Session Management)
- شناسه‌های نشست (Session IDs) با استفاده از تابع تصادفی رمزنگاری امن `crypto.randomBytes(32)` به صورت هگزادسیمال با انتروپی ۲۵۶ بیت تولید می‌شوند.
- در دیتابیس، صرفاً هش SHA-256 توکن نشست ذخیره می‌گردد تا در صورت بروز هرگونه دسترسی غیرمجاز به فایل دیتابیس، توکن‌های زنده قابل سوءاستفاده نباشند.
- کوکی نشست با ویژگی‌های زیر ارسال می‌شود:
  - `HttpOnly`: جلوگیری از هرگونه سرقت کوکی توسط اسکریپت‌های کلاینت (XSS).
  - `SameSite=Lax`: محافظت در برابر جعل درخواست میان‌وب‌گاهی (CSRF).
  - `Secure`: الزام به ارسال صرفاً بر روی بستر امن HTTPS (در محیط Production).
- تایم‌اوت‌های نشست:
  - انقضای کلی: ۸ ساعت.
  - انقضای عدم فعالیت (Idle Timeout): ۴۵ دقیقه.
- قابلیت **خاتمه اضطراری نشست‌ها** (Session Revocation) به صورت تکی یا جمعی توسط خود کاربر.

---

## ۳. کنترل دسترسی نقش‌محور (RBAC)

سامانه از ۴ نقش تفکیک‌شده با اصل حداقل دسترسی (Principle of Least Privilege) پشتیبانی می‌کند:

| نقش (Role) | عنوان فارسی | اختیارات و مجوزها (Permissions) |
|---|---|---|
| `super_admin` | مدیر ارشد سیستم | دسترسی کامل، مدیریت تمام کاربران و نقش‌ها، مشاهده کامل لاگ‌ها، تنظیمات پیشرفته |
| `admin` | مدیر محتوا و فایل | مدیریت محتوا (`content:write`, `content:publish`), آپلود و مدیریت فایل‌ها (`files:upload`, `files:delete`) |
| `editor` | نویسنده و ویرایشگر | ایجاد و ویرایش محتوا (`content:write`, `content:read`) |
| `file_manager` | مدیر رسانه | صرفاً آپلود، مشاهده و سازماندهی فایل‌ها (`files:upload`, `files:read`) |

تمام مسیرهای API در سمت سرور توسط میان‌افزارهای `requirePermission` و `requireRole` اعتبارسنجی می‌شوند. هیچ تصمیمی صرفاً بر عهده فرانت‌اند واگذار نشده است.

---

## ۴. محافظت در برابر حملات متداول وب

### الف. جلوگیری از Brute Force و Credential Stuffing
- شمارنده تلاش‌های ناموفق: پس از ۵ مرتبه ورود ناموفق، حساب کاربری به مدت ۱۵ دقیقه مسدود (Lockout) می‌گردد.
- محدودکننده نرخ درخواست (Rate Limiting) با `express-rate-limit`: حداکثر ۱۰ تلاش ورود در هر ۱۵ دقیقه برای هر آدرس IP.

### ب. محافظت کامل در برابر آپلود فایل‌های مخرب
1. **بررسی امضای باینری (Magic Bytes):** فایل‌ها تنها بر اساس پسوند سنجیده نمی‌شوند؛ بلکه بایت‌های جادویی ابتدای فایل با هدرهای معتبر (JPEG, PNG, GIF, PDF, ZIP, WebP) مقایسه می‌گردند.
2. **مسدودسازی پسوندهای اجرایی و پسوندهای دوگانه:** پسوندهای `.php`, `.phtml`, `.exe`, `.sh`, `.js`, `.py` و فایل‌های فریبنده نظیر `image.php.jpg` به طور قطعی رد می‌شوند.
3. **پاکسازی فایل‌های SVG:** فایل‌های وکتور جهت خنثی‌سازی حملات Stored XSS بررسی شده و هرگونه تگ `<script>` یا رویداد `onload`/`onerror` حذف یا رد می‌شود.
4. **تغییر نام تصادفی و ایزولاسیون:** فایل‌ها با یک شناسه تصادفی UUID و برچسب زمانی تغییر نام می‌یابند و در پوشه اختصاصی `/uploads` خارج از روت اصلی فرانت‌اند نگهداری می‌شوند تا امکان اجرای مستقیم کد وجود نداشته باشد.
5. **جلوگیری از Path Traversal:** شناسه‌ها با توابع `path.resolve` و اعتبارسنجی کاراکترهای `..` و Null-byte (`\0`) فیلتر می‌شوند.

### ج. محافظت در برابر CSRF
- بهره‌گیری از الگوریتم Double Submit Cookie Pattern همراه با هدر اختصاصی `x-csrf-token`.
- هر درخواست تغییر وضعیت (POST, PUT, DELETE) مستلزم ارسال توکن معتبر تطبیق‌یافته با کوکی مرورگر است.

### د. هدرهای امنیتی (HTTP Security Headers)
- پیکربندی شده توسط Helmet:
  - `Content-Security-Policy`: محدودسازی منابع لود اسکریپت، استایل و فونت‌ها.
  - `Strict-Transport-Security` (HSTS): اجبار بر ارتباط دائم رمزگذاری شده.
  - `X-Content-Type-Options: nosniff`: جلوگیری از تفسیر نادرست نوع فایل‌ها توسط مرورگر.
  - `Permissions-Policy`: سلب دسترسی به دوربین، میکروفون و موقعیت مکانی ناخواسته.
  - `X-Frame-Options` / `frame-ancestors`: محافظت در برابر حملات Clickjacking.

---

## ۵. لاگ‌های امنیتی و ممیزی (Audit Logging)

کلیه رویدادهای حساس سیستمی به صورت دائم در جدول `audit_logs` ثبت می‌شوند، شامل:
- ورودهای موفق و ناموفق (با ثبت IP و User-Agent).
- قفل شدن حساب کاربری به دلیل نقض تعداد تلاش‌ها.
- تغییر رمز عبور یا راه‌اندازی ۲FA.
- ایجاد، ویرایش، انتشار یا حذف آیتم‌های محتوا.
- آپلود، دانلود فایل‌های خصوصی یا حذف رسانه‌ها.
- تغییر نقش یا وضعیت فعال/غیرفعال بودن کاربران.

> **محرمانگی لاگ‌ها:** سیستم ممیزی به صورت خودکار فیلدهای حساس مانند `password`, `token`, `secret`, و `mfaToken` را با مقدار `[REDACTED]` جایگزین می‌کند تا هیچ‌گاه اسرار امنیتی در پایگاه‌داده ذخیره نشوند.

---

## ۶. چک‌لیست استقرار در محیط عملیاتی (Production Deployment Checklist)

قبل از انتقال سامانه به سرور عملیاتی، موارد زیر را بررسی نمایید:

1. [ ] فایل `.env` را ایجاد کرده و مقادیر `SESSION_SECRET` و `CSRF_SECRET` را با عبارات تصادفی حداقل ۶۴ کاراکتری پر کنید.
2. [ ] متغیر `NODE_ENV=production` را تنظیم نمایید تا کوکی‌های `Secure` فعال شده و پیام‌های خطای سرور به طور کامل انتزاعی شوند.
3. [ ] پس از اولین ورود با کاربر Super Admin، فوراً رمز عبور پیش‌فرض را تغییر دهید.
4. [ ] ورود دو مرحله‌ای (TOTP 2FA) را برای حساب مدیر ارشد فعال کنید و کدهای پشتیبان را در محل امن ذخیره نمایید.
5. [ ] از پوشه `/uploads` و فایل پایگاه‌داده دیتابیس پشتیبان‌گیری دوره‌ای (Backup Cron) تهیه نمایید.
6. [ ] گواهی SSL/TLS (HTTPS) معتبر روی دامنه مستقر باشد.

---

## ۷. فرآیند گزارش آسیب‌پذیری (Vulnerability Reporting)

در صورت کشف هرگونه نقص امنیتی یا باگ در سامانه، لطفاً موضوع را از طریق ایمیل امن مدیر سیستم گزارش فرمایید و از طرح عمومی آن در محیط‌های عمومی خودداری نمایید. گزارش‌ها در اولویت اول بررسی و وصله خواهند شد.
